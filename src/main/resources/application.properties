# application.properties
spring.application.name=barberbooking

# default profile
spring.profiles.default=dev
spring.profiles.active=dev


# Default server (for dev)
server.port=8080

# Service name
servicename=BarberBooking
